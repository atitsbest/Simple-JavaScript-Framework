/**
* @namespace Tests
*/
if (Object.isUndefined(Tests) || !Tests) { var Tests = {}; }

describe('ImportFilter', {
  'before': function() {
    
  },
  'should be created': function() {
    value_of(sut).should_not_be(null);
  }
})

